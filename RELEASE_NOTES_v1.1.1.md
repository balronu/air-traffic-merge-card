# v1.1.1

Compatibility update for the current Air Traffic Merge integration.

## Fixed

- Corrected the example entity to `sensor.air_traffic_merged`.
- Pointed HACS to `dist/air-traffic-merge-card.js`.
- Added support for the current integration attributes:
  - `source`
  - `aircraft_model`
  - tracked flight flags

## Added

- Validation workflow for JavaScript syntax and JSON files.
